
# secrets live here — never commit them (added by agent-standard adopt)
.env
.env.*
!.env.example

# live AgentMesh runtime data (per docs/archive/PROJECT-SETUP.md) — never track agent state/messages/locks
.tmp_*
*.lock
!uv.lock
agents/
locks/
config/*.json
!config/*.template.json

# Python packaging/tooling
.venv/
.uv-cache/
__pycache__/
*.egg-info/
.pytest_cache/
.ruff_cache/

# OS junk
.DS_Store
Thumbs.db
